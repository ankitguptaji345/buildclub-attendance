// config.js
// Simple settings file. Change ADMIN_PASSWORD to whatever your team wants
// before your demo - this is the password needed to register a new face.
//
// Tip: you can also set it via an environment variable instead of editing
// this file, e.g. on Mac/Linux:  ADMIN_PASSWORD=mysecret npm start

module.exports = {
  ADMIN_PASSWORD: process.env.ADMIN_PASSWORD || 'buildclub2026'
};
